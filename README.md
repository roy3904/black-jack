# black-jack

Up to February 25th, 2025: Implemented the Card and Deck classes, as well as added implementation to generate initial deck, and shuffle it. Also added the dealer class and added some functionality
February 25th, 2025: Troubleshooted the data structure, as it was having trouble with the discard function inside Dealer that transfered cards away from the hand vector and into a deck object, the discard pile. Will begin working on implementation to reshuffle cards from discard pile back into the main deck once the amount of cards in the main deck runs low. Will likely do an if statement, doing one case if the deck size is only 1 deck of cards, and another if there are multiple decks composed in the main deck. This is determined by the constant variable DECK_COUNT

April 24th, 2025: Fixed up the hand structure inside of the dealer class, as I discovered it wouldn't be satisfactory to handle a black jack hand. I needed the ability to stack seperate hands, so I implemented an array where each index holds a vector that stores the cards themselves. This also involved reworking the functions that manipulated this hand, but this wasn't as bad as I expected as it was mostly implementing a for loop for each function. I also began implementing the main menu where there are 3 options: playing the game (currently implementing), changing game rules (this is where the fun stuff will be), and the quit option. Lastly, I began implementing the play round function but as of today this was mainly used for testing my data structure changes to Dealer.
